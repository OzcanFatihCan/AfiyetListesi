import 'package:flutter/material.dart';

class AlternativeLoginPageView extends StatelessWidget {
  const AlternativeLoginPageView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
